import React from "react";
import "../styles.css"; // Update the import path

const Header = () => {
  return (
    <div class="title-container">
        <div class="glow-box">Smart Voice Assistant</div>
    </div>
  );
};

export default Header;